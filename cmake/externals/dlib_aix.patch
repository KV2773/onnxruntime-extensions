--- a/dlib/external/libpng/pngpriv.h
+++ b/dlib/external/libpng/pngpriv.h
@@ -196,7 +196,7 @@
 #endif

 #ifndef PNG_POWERPC_VSX_OPT
-#  if defined(__PPC64__) && defined(__ALTIVEC__) && defined(__VSX__)
+#  if (defined(_AIX) || defined(__PPC64__)) && defined(__ALTIVEC__) && defined(__VSX__)
 #     define PNG_POWERPC_VSX_OPT 2
 #  else
 #     define PNG_POWERPC_VSX_OPT 0
--- a/dlib/CMakeLists.txt
+++ b/dlib/CMakeLists.txt
@@ -500,6 +500,15 @@
                   )
                set_source_files_properties(external/libpng/arm/filter_neon.S PROPERTIES COMPILE_FLAGS "${CMAKE_ASM_FLAGS} ${CMAKE_CXX_FLAGS} -x assembler-with-cpp")
             endif()
+
+            # Add PowerPC VSX support for AIX
+            if (CMAKE_SYSTEM_NAME MATCHES "AIX" AND CMAKE_SYSTEM_PROCESSOR MATCHES "powerpc")
+               message (STATUS "PowerPC VSX instructions will be used for libpng.")
+               set(source_files ${source_files}
+                  external/libpng/powerpc/powerpc_init.c
+                  external/libpng/powerpc/filter_vsx_intrinsics.c
+                  )
+            endif()
          endif()
          set(source_files ${source_files}
             image_loader/png_loader.cpp

# Made with Bob
